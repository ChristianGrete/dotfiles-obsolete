#!/bin/sh

cd `dirname $0`/..

. "`pwd`/script/setup.sh"

for script in splash boot init verify update link
  do
    . "$SCRIPTS/$script.sh"
done

unset script

echo 'Dotfiles successfully deployed!'
echo