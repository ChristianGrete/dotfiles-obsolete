#!/bin/sh

cd "$(dirname "$0")/.."

if [ -z ${PWD:+PWD} ] || [ "$PWD" != "$(pwd)" ]
  then
    export PWD="$(pwd)"
fi

ROUTINES="$PWD/lib/install"

# for routine in foo bar baz
#   do
#     . "$ROUTINES/$routine.sh"
# done
