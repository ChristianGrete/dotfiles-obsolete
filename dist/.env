# Default text editors
export EDITOR="$(command -v -- ex)"
export VISUAL="$(command -v -- vi)"

# Path to additional profiles
export PROFILES="$HOME/.profiles"

# Path to workspace
export PROJECTS="$HOME/.projects"
